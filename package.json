{
  "name": "proteus-tutorial",
  "version": "1.0.0",
  "license": "MIT",
  "description": "This repo tests the ability to import and use components from the fluent-ui-react library.",
  "repository": "https://github.com/astegmaier/fluent-ui-react-test",
  "author": "Andrew Stegmaier <ansteg@microsoft.com>",
  "scripts": {
    "start": "concurrently \"tsc --watch\" \"parcel src/index.html --open\"",
    "build": "tsc && rimraf dist && parcel build src/index.html --no-source-maps",
    "check": "tsc"
  },
  "dependencies": {
    "@fluentui/react": "^0.43.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "@types/react": "^16.9.17",
    "@types/react-dom": "^16.9.4",
    "concurrently": "^5.0.2",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.5"
  }
}
